tags:
  - Sections
summary: Retrieves a specific section by ID
security:
  - Bearer: []
parameters:
  - name: section_id
    in: path
    description: ID of the section to fetch
    required: true
    type: string
responses:
  200:
    description: Section object found
    schema:
      $ref: '#/definitions/Section'
  404:
    description: Section not found
