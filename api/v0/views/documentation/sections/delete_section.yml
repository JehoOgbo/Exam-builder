tags:
  - Sections
summary: Deletes a section object
security:
  - Bearer: []
parameters:
  - name: section_id
    in: path
    description: ID of the section to delete
    required: true
    type: string
responses:
  200:
    description: Section deleted (returns empty JSON)
  404:
    description: Section not found
